
% This LaTeX was auto-generated from an M-file by MATLAB.
% To make changes, update the M-file and republish this document.



    
    

\subsection*{Rbased\_loss\_hc.m} 

\begin{verbatim}function [L, dLdm, dLds, S2] = Rbased\_loss\_hc(cost, m, s)\end{verbatim}
\begin{par}
\textbf{Input arguments:}
\end{par} \vspace{1em}
\begin{verbatim}cost            cost structure
 .rewardgpmodel GP to predict reward given state
m               mean of state distribution                      [D x  1 ]
s               covariance matrix for the state distribution    [D x  D ]\end{verbatim}
\begin{par}
\textbf{Output arguments:}
\end{par} \vspace{1em}
\begin{verbatim}L     expected cost                                             [1 x  1 ]
dLdm  derivative of expected cost wrt. state mean vector        [1 x  D ]
dLds  derivative of expected cost wrt. state covariance matrix  [1 x D^2]
S2    variance of cost                                          [1 x  1 ]\end{verbatim}

\begin{lstlisting}
function [L, dLdm, dLds, S2] = Rbased_loss_hc(cost, m, s)
\end{lstlisting}


\subsection*{Code} 


\begin{lstlisting}
	[L, S2, ~, dLdm, ~, ~, dLds, ~, ~] = gp1d(cost.rewardgpmodel, m, s);
\end{lstlisting}

\begin{lstlisting}
end
\end{lstlisting}
